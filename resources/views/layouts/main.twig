<!DOCTYPE html>
<html lang="{{ current_language }}">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<meta name="description" content="{{ description|default(trans('home.description')) }}">
		<title>{{ title|default(trans('home.title')) }}
			-
			{{ app_name }}</title>

		{# Favicon #}
		<link rel="icon" type="image/x-icon" href="/assets/img/favicon/favicon.ico">
		<link rel="icon" type="image/png" sizes="32x32" href="/assets/img/favicon/favicon-32x32.png">
		<link rel="icon" type="image/png" sizes="16x16" href="/assets/img/favicon/favicon-16x16.png">
		<link rel="apple-touch-icon" sizes="180x180" href="/assets/img/favicon/apple-touch-icon.png">
		<link
		rel="manifest" href="/assets/img/favicon/site.webmanifest">

		{# Bootstrap CSS #}
		<link
		href="/assets/libs/bootstrap/bootstrap.min.css" rel="stylesheet">

		{# Font Awesome Icons #}
		<link
		rel="stylesheet" href="/assets/libs/fontawesome/css/all.min.css">

		{# Seu CSS customizado #}
		<link rel="stylesheet" href="/assets/css/style.css"> {% block styles %}{% endblock %}
		</head>
		<body>
			<nav class="navbar navbar-expand-lg">
				<div class="container-fluid">
					<a href="{{ route('') }}" class="navbar-brand">
						<img src="/assets/img/logo.png" alt="{{ app_name }}" width="90" height="72">
					</a>
					<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
						<span class="navbar-toggler-icon"></span>
					</button>
					<div class="collapse navbar-collapse justify-content-md-center" id="navbarNavDropdown">
						<ul class="navbar-nav">
							<li class="nav-item">
								<a class="nav-link active" aria-current="page" href="{{ route('') }}">{{ trans('nav.home') }}</a>
							</li>
							<li class="nav-item">
								<a class="nav-link" href="{{ route('sobre') }}">{{ trans('nav.about') }}</a>
							</li>
							<li class="nav-item">
								<a class="nav-link" href="{{ route('contato') }}">{{ trans('nav.contact') }}</a>
							</li>
							<li class="nav-item dropdown">
								<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
									{{ trans('nav.projects') }}
								</a>
								<ul class="dropdown-menu">
									<li>
										<a class="dropdown-item" href="{{ route('projetos') }}">{{ trans('nav.projects') }}</a>
									</li>
									<li>
										<a class="dropdown-item" href="{{ route('apirest') }}">{{ trans('nav.apirest') }}</a>
									</li>
									<li>
										<a class="dropdown-item" href="{{ route('gestao') }}">{{ trans('nav.gestao') }}</a>
									</li>
								</ul>
							</li>
						</ul>
					</div>


					{# Seletor de idioma #}
					<div class="language-selector">
						<button class="language-btn" id="languageBtn">
							<i class="fas fa-globe"></i>
							<span>{{ current_language|upper }}</span>
						</button>
						<div class="language-dropdown" id="languageDropdown">
							{% for lang in available_languages %}
								<a href="/set-language/{{ lang }}" class="language-option {% if lang == current_language %}active{% endif %}">
									<span class="flag">{{ lang == 'pt' ? '🇧🇷' : (lang == 'en' ? '🇺🇸' : '🇪🇸') }}</span>
									<span>{{ lang == 'pt' ? 'Português' : (lang == 'en' ? 'English' : 'Español') }}</span>
								</a>
							{% endfor %}
						</div>
					</div>
				</div>
			</nav>

			<main>
				{% block content %}{% endblock %}
			</main>

			<footer class="footer">
				<div class="container">
					<div class="footer-content">
						<div class="footer-section">
							<h3>{{ app_name }}</h3>
							<p>{{ trans('footer.description') }}</p>
						</div>
						<div class="footer-section">
							<h3>{{ trans('footer.quick_links') }}</h3>
							<ul>
								<li><ahref="{{route('')}}">{{ trans('nav.home') }}</a></li>
								<li><ahref="{{route('projetos')}}">{{ trans('nav.projects') }}</a></li>
								<li><ahref="{{route('sobre')}}">{{ trans('nav.about') }}</a></li>
								<li><ahref="{{route('contato')}}">{{ trans('nav.contact') }}</a></li>
							</ul>
						</div>
						<div class="footer-section">
							<h3>{{ trans('footer.social') }}</h3>
							<div class="social-links">
								<a href="#" target="_blank">GitHub</a>
								<a href="#" target="_blank">LinkedIn</a>
								<a href="mailto:dorianc.vieira@gmail.com">Email</a>
							</div>
						</div>
					</div>
					<div class="footer-bottom">
						<p>&copy;
							{{ "now"|date("Y") }}
							{{ app_name }}.
							{{ trans('footer.rights') }}.</p>
					</div>
				</div>
			</footer>

			{# Bootstrap Bundle (inclui Popper) #}
			<script src="/assets/libs/bootstrap/bootstrap.bundle.min.js"></script>

			{# Seu JS customizado #}
			<script src="/assets/js/script.js"></script>

			{% block scripts %}{% endblock %}
		</body>
	</html>
